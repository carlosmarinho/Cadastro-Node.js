<% if (errors) {%>
  <% errors.forEach(function (error) { %>
    <%= __(error) %>
  <% }) %>
<% } %>

<form role="form" action="/auth/local" method="post">
  <h2 class="form-signin-heading"> Faça o seu login! </h2>
      <br />
  <input type="text" name="identifier" placeholder="Username ou Email"><br />
  <input type="password" name="password" placeholder="Senha"><br />
  <br />
  <button type="submit">Logar</button>
</form>

<% if (Object.keys(providers).length) {%>
  <h4>You can also use one of these...</h4>

  <% Object.keys(providers).forEach(function (key) { %>
    <a href="/auth/<%= providers[key].slug %>" role="button"><%= providers[key].name %></a>
  <% }) %>
<% } %>
